# Cline install

Copy `skills/cogsend/` from this repo into your workspace `.cline/skills/`
directory, or into your global Cline skills directory.

Set `APP_URL` and `COGSEND_API_KEY` in your environment so the skill can reach
your cogsend instance.
